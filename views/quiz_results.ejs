<!DOCTYPE html>
<html>
<head>
    <title>Quiz Results</title>
    <link rel="stylesheet" href="/css/quiz_results.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <header>
        <h1>Quiz Results</h1>
    </header>
    <div class="container">
        <h2>Your Score: <%= score %>/<%= totalQuestions %></h2>

        <h2>Questions and Answers:</h2>
        <ul>
            <% resultDetails.forEach((result, index) => { %>
                <li>
                    <strong>Question <%= index + 1 %>:</strong> <%= result.question_text %><br>
                    <strong>Options:</strong><br>
                    A: <%= result.options.a %><br>
                    B: <%= result.options.b %><br>
                    C: <%= result.options.c %><br>
                    D: <%= result.options.d %><br>
                    <strong>Your Answer:</strong> <%= result.userAnswer %> (<%= result.isCorrect ? 'Correct' : 'Incorrect' %>)<br>
                    <% if (!result.isCorrect) { %>
                        <strong>Correct Answer:</strong> <%= result.correctAnswer %><br>
                    <% } %>
                </li>
            <% }) %>
        </ul>

        <a href="/student/dashboard">Back to Dashboard</a>
    </div>
    <footer>
        &copy; 2024 Group 1 System & Network Administration. All Rights Reserved.
    </footer>
</body>
</html>
